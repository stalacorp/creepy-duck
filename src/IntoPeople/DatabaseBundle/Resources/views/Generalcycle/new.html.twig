{% extends '::base.html.twig' %} 

{% block body -%}

    <h2>{{ "generalcycle.new.title" | trans }}</h2>

    <div class="row">
        <div class="col-md-6">

            {% for flashMessage in app.session.flashbag.get('error') %}

                <div class="alert alert-error">
                    <button class="close" data-dismiss="alert"></button>
                    {{ flashMessage }}
                </div>
            {% endfor %}

            {{ form_start(form) }}

            <h4>General information</h4>

            <div class="row">

                <div class="col-md-6">

                    <p><em>Pick a year.</em></p>

                    {{ form_row(form.year) }}


                </div>

            </div>

            <h4Career development plan</h3>

            <p><em>When will your career development plan be available and when is the deadline.</em></p>

            <div class="row">

                <div class="col-md-6">
                    {{ form_row(form.startdatecdp) }}
                </div>

                <div class="col-md-6">
                    {{ form_row(form.enddatecdp) }}
                </div>

            </div>

            <h4>Mid year</h4>

            <div class="row">

                <div class="col-md-6">
                    {{ form_row(form.startdatemidyear) }}
                </div>

                <div class="col-md-6">
                    {{ form_row(form.enddatemidyear) }}
                </div>

            </div>

            <h4>Year end</h4>

            <div class="row form-row">

                <div class="col-md-6">
                    {{ form_row(form.startdateyearend) }}
                </div>

                <div class="col-md-6">
                    {{ form_row(form.enddateyearend) }}
                </div>

            </div>


            {% if is_granted('ROLE_SUPER_ADMIN') %}

                {{ form_row(form.organization) }}

            {% endif %}

            <div class="buttons">{{ form_widget(form.submit) }} <a href="{{ path('generalcycle') }}"
                                                                         class="btn btn-default btn-cons" type="button">Back</a>
            </div>

            {{ form_end(form) }}


        </div>
    </div>


{% endblock %}
